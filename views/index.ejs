<!-- Background image -->
<div id="intro-example" class="p-5 text-center bg-image hero-img ">
  <div class="mask">
    <div class="d-flex justify-content-center align-items-center h-100">
      <div class="text-white">
        <h1 class="mb-3">Discover a World of Recipes</h1>
        <h5 class="mb-4">
          From family classics to international favorites, explore our culinary treasure trove
        </h5>
        <a class="btn btn-outline-light btn-lg m-2" href="explore-latest" role="button" rel="nofollow" target="_blank">Explore
          Latest</a>
        <a class="btn btn-outline-light btn-lg m-2" href="explore-random" role="button" rel="nofollow" target="_blank">Show Random</a>
      </div>
    </div>
  </div>
</div>

<!-- Categories Start -->
<div class="row row-cols-2 row-cols-lg-6 g-2 g-lg-3 py-4">
  <% if(categories !='' ) { %>
    <% categories.forEach(function(category, index) { %>
      <a href="/categories/<%- category.name %>" class="col text-center category__link">
        <div class="category__img shadow">
          <img src="/img/<%- category.image %>" alt="<%- category.name%>" loading="lazy">
        </div>
        <div class="pt-1"><%- category.name %></div>
      </a>
    <% }) %>
  <% } %>
  <a href="/categories" class="col text-center category__link">
    <div class="category__img shadow">
      <img src="/img/view_all.jpeg" alt="View All Categories" loading="lazy">
    </div>
    <div class="pt-1">View All</div>
  </a>
</div>
<!-- Categories end -->

<!-- Latest Recipes -->
<section class="pb-4 pt-4">
  <div class="d-flex mb-2 align-items-center">
    <h2>Latest Recipes</h2>
    <a href="/explore-latest" class="ms-auto">View More</a>
  </div>
  <div class="row row-cols-1 row-cols-md-4 g-4">
    <% if(food != '' ) { %>
      <% food.latest.forEach(function(recipe, index) { %>
        <a href="/recipe/<%= recipe._id %>" class="col text-center category__link">
          <div class="category__img category__img--large shadow" style="overflow: hidden;">
            <img src="/uploads/<%= recipe.image %>" alt="<%= recipe.name %>" loading="lazy" style="object-fit: cover; width: 100%; height: 100%;">
          </div>
          <div class="pt-1"><%= recipe.name %></div>
        </a>
      <% }) %>
    <% } %>
  </div>
</section>
<!-- Latest Recipes END -->
<!-- Thai Recipes -->
<section class="pb-4 pt-4">
  <div class="d-flex mb-2 align-items-center">
    <h2>Thai Recipes</h2>
    <a href="/categories/Thai" class="ms-auto">View More</a>
  </div>
  <div class="row row-cols-1 row-cols-md-4 g-4">
    <% if(typeof food.thai !== 'undefined' && food.thai.length > 0) { %>
      <% food.thai.forEach(function(recipe, index) { %>
        <a href="/recipe/<%= recipe._id %>" class="col text-center category__link">
          <div class="category__img category__img--large shadow" style="overflow: hidden;">
            <img src="/uploads/<%= recipe.image %>" alt="<%= recipe.name %>" loading="lazy" style="object-fit: cover; width: 100%; height: 100%;">
          </div>
          <div class="pt-1"><%= recipe.name %></div>
        </a>
      <% }) %>
    <% } else { %>
      <p>No Items Found.</p>
    <% } %>
  </div>
</section>
<!-- Thai Recipes END -->

<!-- American Recipes -->
<section class="pb-4 pt-4">
  <div class="d-flex mb-2 align-items-center">
    <h2>American Recipes</h2>
    <a href="/categories/American" class="ms-auto">View More</a>
  </div>
  <div class="row row-cols-1 row-cols-md-4 g-4">
    <% if(typeof food.american !== 'undefined' && food.american.length > 0) { %>
      <% food.american.forEach(function(recipe, index) { %>
        <a href="/recipe/<%= recipe._id %>" class="col text-center category__link">
          <div class="category__img category__img--large shadow" style="overflow: hidden;">
            <img src="/uploads/<%= recipe.image %>" alt="<%= recipe.name %>" loading="lazy" style="object-fit: cover; width: 100%; height: 100%;">
          </div>
          <div class="pt-1"><%= recipe.name %></div>
        </a>
      <% }) %>
    <% } else { %>
      <p>No Items Found.</p>
    <% } %>
  </div>
</section>
<!-- American Recipes END -->


<!-- Chinese Recipes -->
<section class="pb-4 pt-4">
  <div class="d-flex mb-2 align-items-center">
    <h2>Chinese Recipes</h2>
    <a href="/categories/Chinese" class="ms-auto">View More</a>
  </div>
  <div class="row row-cols-1 row-cols-md-4 g-4">
    <% if(typeof food.chinese !== 'undefined' && food.chinese.length > 0) { %>
      <% food.chinese.forEach(function(recipe, index) { %>
        <a href="/recipe/<%= recipe._id %>" class="col text-center category__link">
          <div class="category__img category__img--large shadow" style="overflow: hidden;">
            <img src="/uploads/<%= recipe.image %>" alt="<%= recipe.name %>" loading="lazy" style="object-fit: cover; width: 100%; height: 100%;">
          </div>
          <div class="pt-1"><%= recipe.name %></div>
        </a>
      <% }) %>
    <% } else { %>
      <p>No Items Found.</p>
    <% } %>
  </div>
</section>
<!-- Chinese Recipes END -->





<!-- submit-start -->
<section class=" submit px-4 py-5 my-5 text-center">
  <img src="/img/submit.jpg" class="d-block mx-auto mb-4 " alt="submit recipe" width="250" height="250" loading
  lazy">
  <h1 class="display-5 fe-bold">Publish your recipe for FREE today</h1>
  <div class="col-lg-6 mx-auto">
      <p class="lead mb-4">Publish your recipes for the world to see</p>
      <div class="d-grid gap-2 d-sm-flex justify-content-sm-center">
          <a href="/submit-recipe" class="btn btn-primary btn-dark btn-lg">Submit recipe</a>
      </div>
  </div>

</section>

<!-- submit-end -->
